---
- hosts:                rudolph
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'configure sshd on rudolph:  sshd_config'
    assemble:
      remote_src:       False
      src:              '~/code/fleet/conf/ssh/rudolph'
      dest:             '/etc/ssh/sshd_config'
      owner:            'root'
      group:            'root'
      mode:             '0644'
    register:           sshd_config_rudolph
  - name:               'configure sshd on rudolph:  service restart'
    service:
      name:             'ssh'
      state:            'restarted'
    when:               sshd_config_rudolph.changed
