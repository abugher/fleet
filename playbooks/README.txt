This directory is ~/code/fleet/playbooks .

There should be a symlink:  ~/code/ansible  ->  fleet/playbooks


setup_fleet.yml is the top thing.

setup_* are top level things, which might mostly mean experimental
playbooks, which will get rolled into setup_fleet.yml upon completion.
