---
- hosts:                rudolph
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install scripts on rudolph:  nethack'
    copy:
      owner:            'root'
      group:            'root'
      mode:             '0755'
      src:              '~/code/fleet/bin/nethack'
      dest:             '/usr/local/bin/nethack'

