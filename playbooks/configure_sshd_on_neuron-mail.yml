---
- hosts:                neuron-mail
  name:                 'configure sshd on neuron-mail'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install sshd_config'
    assemble:
      remote_src:       False
      src:              '~/code/fleet/conf/ssh/neuron-mail'
      dest:             '/etc/ssh/sshd_config'
      owner:            'root'
      group:            'root'
      mode:             '0644'
    register:           sshd_config_neuron_mail
  - name:               'restart service'
    service:
      name:             'ssh'
      state:            'restarted'
    when:               sshd_config_neuron_mail.changed
