---
- name:                 'stop services'
#  service:
#    name:               "{{ item }}"
#    state:              'stopped'
#  with_items:           "{{ enable_services }}"
  command:              service "{{ item }}" stop
  with_items:           "{{ stop_services }}"
