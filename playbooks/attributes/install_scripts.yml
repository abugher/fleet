---
  - name:               'install scripts'
    copy:
      owner:            "{{ item.owner }}"
      group:            "{{ item.group }}"
      mode:             "{{ item.mode }}"
      src:              "{{ item.src }}"
      dest:             "{{ item.dest }}"
    with_items:         "{{ install_scripts }}"
