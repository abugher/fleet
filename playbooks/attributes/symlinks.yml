---
- name:                 'symlinks'
  file:
    owner:              "{{ item.owner }}"
    group:              "{{ item.group }}"
    mode:               "{{ item.mode }}"
    src:                "{{ item.src }}"
    dest:               "{{ item.dest }}"
    state:              'link'
  with_items:           "{{ symlinks }}"
