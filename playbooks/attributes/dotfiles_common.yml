---
  - name:               'dotfiles, common to all'
    copy:
      src:              '~/code/fleet/conf/dotfiles/common/'
      dest:             "~{{ item }}/"
    with_items:         "{{ dot_users }}"
