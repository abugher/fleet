---
  - name:               'install sshd_config'
    assemble:
      remote_src:       False
      src:              "~/code/fleet/conf/ssh/{{ inventory_hostname }}"
      dest:             '/etc/ssh/sshd_config'
      owner:            'root'
      group:            'root'
      mode:             '0644'
