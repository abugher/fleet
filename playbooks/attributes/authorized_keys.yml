---
  - name:               'authorize keys'
    authorized_key:
      user:             "{{ item.user }}"
      key:              "{{ lookup('file', item.keyfile) }}"
    with_items:         "{{ keys }}"

