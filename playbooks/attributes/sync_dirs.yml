---
  # Work around bug in synchronize 2.0.0.
  - name:               'synchronize directories'
    synchronize:
      src:              "{{ item.src }}"
      dest:             "{{ item.dest }}"
      delete:           "{{ item.delete }}"
    with_items:         "{{ sync_dirs }}"

