---
install_packages:
  - 'libpam-krb5'
groups:                 "{{ users }}"
homedirs:               "{{ users }}"
users:
  - 'aaron'
  - 'rachel'
system_users:
  - 'nagios'
authorized_keys:
  - user:               'nagios'
    keyfile:            '~/code/fleet/conf/ssh_keys/nagios@synapse.id_rsa.pub'

