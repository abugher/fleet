---
users:
  - 'aaron'
  - 'rachel'
homedirs:               "{{ users }}"
groups:                 "{{ users }}"
system_users:
  - 'nagios'
authorized_keys:
  - user:               'nagios'
    keyfile:            '~/code/fleet/conf/ssh_keys/nagios@synapse.id_rsa.pub'

