---
install_files:
  - owner:              'root'
    group:              'root'
    mode:               '0644'
    src:                "../conf/{{ inventory_hostname }}/dphys-swapfile"
    dest:               '/etc/dphys-swapfile'
restart_services:
  - 'dphys-swapfile'
