---
install_files:
  - owner:              'root'
    group:              'root'
    mode:               '0644'
    src:                "roles/reboot/files/{{ inventory_hostname }}/cron.d/reboot"
    dest:               '/etc/cron.d/reboot'
