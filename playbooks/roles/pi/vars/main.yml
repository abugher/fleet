---
# pi user doesn't need to be created; it comes with Raspbian.
authorized_keys:
  - user:               'pi'
    keyfile:            '~/code/fleet/conf/ssh_keys/aaron-phone@aaron-phone.id_rsa.pub'
dot_users:
  - 'pi'
