---
- hosts:                synapse
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'configure nightly reboot on synapse'
    copy:
      src:              '~/code/fleet/cron.d/synapse_reboot'
      dest:             '/etc/cron.d/reboot'
      owner:            'root'
      group:            'root'
      mode:             '0644'

