---
- hosts:                neuron-mail
  name:                 'configure ssh on neuron-mail'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars:
    keys:
      - user:           'aaron'
        keyfile:        '~/code/fleet/conf/ssh_keys/aaron-phone@aaron-phone.id_rsa.pub'
  tasks:

  - include:            'tasks/authorized_keys.yml'
