---
- hosts:                synapse
  name:                 'configure synapse'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars_files:
    - '~/code/sensitive/playbooks/configure_kdc_db_master_key.yml'
  roles:
    #- 'nginx'
    #- 'apt'
    - 'nagios'
    - 'smokeping'
    - 'krb5'
    - 'zabbix'
    - 'ansible'
    - 'reboot'
    # Eventually the synapse role will factor out.
    # Probably.
    - 'synapse'
