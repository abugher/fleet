---
- hosts:                fleet
  name:                 'configure ssh on fleet'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars:
    keys:
      - user:           'ansible'
        keyfile:        '~/code/fleet/conf/ssh_keys/pi@synapse.id_rsa.pub'
  tasks:

  - include:            'attributes/authorized_keys.yml'
