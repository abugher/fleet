---
- hosts:                neuron
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'configure scripts on neuron:  cron.d/tend_storage'
    copy:
      owner:            'root'
      group:            'root'
      mode:             '0644'
      src:              '~/code/fleet/cron.d/tend_storage'
      dest:             '/etc/cron.d/tend_storage'
