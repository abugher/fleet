---
- hosts:                identity_clients
  name:                 'configure groups on identity clients'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars:
    groups:
      - 'aaron'
      - 'rachel'
  tasks:

  - include:              'generics/configure_groups.yml'
