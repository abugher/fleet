---
- hosts:                fleet
  name:                 'install scripts on fleet'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install stamp'
    copy:
      owner:            root
      group:            root
      mode:             0755
      src:              ~/code/fleet/bin/stamp
      dest:             /usr/local/bin/stamp
