---
- hosts:                identity_clients
  name:                 'configure identity_clients'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  roles:
    - 'identity_clients'
