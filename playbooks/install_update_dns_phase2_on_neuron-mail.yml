---
- hosts:                neuron-mail
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install update_dns_phase2 on neuron-mail'
    copy:
      owner:            root
      group:            root
      mode:             0755
      src:              ~/code/fleet/bin/update_dns_phase2
      dest:             /usr/local/bin/update_dns_phase2
