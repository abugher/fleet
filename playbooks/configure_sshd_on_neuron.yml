---
- hosts:                neuron
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'configure sshd on neuron:  sshd_config'
    assemble:
      remote_src:       False
      src:              '~/code/fleet/conf/ssh/neuron'
      dest:             '/etc/ssh/sshd_config'
      owner:            'root'
      group:            'root'
      mode:             '0644'
    register:           sshd_config_neuron
  - name:               'configure sshd on neuron:  service restart'
    service:
      name:             'ssh'
      state:            'restarted'
    when:               sshd_config_neuron.changed
