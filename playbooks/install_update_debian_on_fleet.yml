---
- hosts:                fleet
  name:                 'install update_debian on fleet'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install update_debian'
    copy:
      owner:            root
      group:            root
      mode:             0755
      src:              ~/code/fleet/bin/update_debian
      dest:             /usr/local/bin/update_debian
