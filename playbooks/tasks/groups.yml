---
- name:                 'configure groups'
  group:
    name:               "{{ item }}"
    state:              'present'
  with_items:           "{{ groups }}"
  register:             groups_result
