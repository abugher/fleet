---
- name:                 'stop services'
#  service:
#    name:               "{{ item }}"
#    state:              'stopped'
#  with_items:           "{{ stop_services }}"
  command:              service "{{ item }}" stop
  with_items:           "{{ stop_services }}"
  register:             stop_services_result
