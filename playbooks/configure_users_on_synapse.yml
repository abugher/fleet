---
- hosts:                synapse
  name:                 'configure users on synapse'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'dotfiles for pi'
    copy:
      src:              '~/code/fleet/conf/dotfiles/common/'
      dest:             '~pi/'

