---
- hosts:                neuron
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  tasks:

  - name:               'install dotfiles on neuron'
    copy:
      owner:            'pi'
      group:            'pi'
      mode:             '0644'
      src:              '~/code/fleet/conf/pi.bashrc'
      dest:             '~pi/.bashrc'
