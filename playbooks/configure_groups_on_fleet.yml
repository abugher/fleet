---
- hosts:                fleet
  name:                 'configure groups on fleet'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars:
    groups:
      - 'nagios'
  tasks:

  - include:              'generics/configure_groups.yml'
