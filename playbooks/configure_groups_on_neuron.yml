---
- hosts:                neuron
  name:                 'configure groups on neuron'
  gather_facts:         False
  remote_user:          ansible
  become:               yes
  become_method:        sudo
  vars:
    groups:
      - 'rachel-phone'
      - 'aaron-phone'
    system_groups:
      - 'escrow'
      - 'cryptkeeper'
  tasks:

  - include:            '../generics/configure_groups.yml'
  - include:            '../generics/configure_system_groups.yml'
