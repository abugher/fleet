---
- name:                 "create users"
  user:
    name:               "{{ item }}"
    group:              "{{ item }}"
    # I'm leaving this line commented, to find later when I realize
    # something is broken.  Create and assign these groups as needed,
    # instead of assuming they all exist and shotgunning them.
    #groups:             'cdrom,floppy,audio,dip,video,plugdev,scanner,netdev,lpadmin'
    comment:            ""
    state:              "present"
    shell:              "/bin/bash"
    password:           "*"
  with_items:           "{{ users }}"
  register:             users_result
