---
mac_address:            '62:77:D3:55:8A:71'
ip_address:             '192.168.11.58'
parent:                 "{{ gateway }}"
platform:               'laptop'
monitor:                'yes'
notify:                 'no'
backup_users:
  - 'rachel'
# The vars file in backup_client role can't loop over backup_users.  So there's
# this, which clearly only works as long as we're only doing one user.
backup_user:            'rachel'
allow_users:
  - 'aaron'
  - 'rachel'
