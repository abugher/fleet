---
- name:                 'logging'
  hosts:
    - "{{ syslog_server }}"
  vars:
#    skipdeps:           'any_value_means_true'
  remote_user:          'ansible'
  become:               'yes'
  become_method:        'sudo'
  gather_facts:         'no'
  roles:
    - 'syslog_server'
