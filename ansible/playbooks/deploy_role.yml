---
- name:                 "deploy role:  {{ role }}"
  remote_user:          'ansible'
  become:               'yes'
  become_method:        'sudo'
  gather_facts:         'no'
  hosts:                "{{ role }}"
  roles:
    - "{{ role }}"
