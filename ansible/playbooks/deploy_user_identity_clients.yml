---
- name:                 'deploy user identity client'
  vars:
    role_name:          'user_identity_client'
  remote_user:          'ansible'
  become:               'yes'
  become_method:        'sudo'
  gather_facts:         'no'
  hosts:                "{{ role_name }}"
  roles:
    - "{{ role_name }}"
