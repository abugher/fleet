---
- name:                 'hack'
  hosts:                elk
  vars:
#    skipdeps:           'anyvalue'
  remote_user:          'ansible'
  become:               'yes'
  become_method:        'sudo'
  gather_facts:         'no'
  roles:
    - 'elk'
