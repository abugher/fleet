---
- vars:
    role_name:          'ansible_target'
  name:                 "deploy {{ role_name }}"
  remote_user:          'ansible'
  become:               'yes'
  become_method:        'sudo'
  gather_facts:         'no'
  hosts:                "{{ role_name }}"
  roles:
    - "{{ role_name }}"
