---
- raw:                  if ! test -e /etc/python_is_installed.txt; then apt-get update && apt-get -y install python > /var/log/install_python.log 2>&1 && touch /etc/python_is_installed.txt; fi
  args:
    executable:         '/bin/bash'
- include:              '../tasks/system_groups.yml'
- include:              '../tasks/system_users.yml'
- include:              '../tasks/authorized_keys.yml'
