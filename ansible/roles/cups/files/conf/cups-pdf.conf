Out /usr/local/share/print_target
AnonDirName /usr/local/share/print_target
UserUMask 0113
Grp lpadmin
DecodeHexStrings 1
Listen 0.0.0.0:631

#ServerKey /etc/ssl/private/{{ inventory_hostname }}.pem
#ServerCertificate /etc/ssl/certs/{{ inventory_hostname }}.pem

CreateSelfSignedCerts no

# Default:
# ServerKeychain /etc/cups/ssl
