#!/bin/bash

# DISTRIBUTED BY ANSIBLE

if test $# != 1; then
  echo -e "Usage:\n  $0 <server>" >&2
  exit 1
fi

server=$1

# Create session.
ssh -t $server 'x11vnc -display :0 -forever -ssl -sslverify ~/.vnc_ssl_keys/'$USER


